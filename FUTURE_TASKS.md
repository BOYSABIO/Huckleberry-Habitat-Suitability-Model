# Future tasks (after TASK.md)

Complete [TASK.md](TASK.md) first (Docker API + MLflow). Then consider:

1. **DVC** — dataset versioning, remote storage, reproducible ETL pipeline (`dvc repro`)
2. **W&B** — optional experiment UI if MLflow dashboards are not enough
3. **Location-based API** — `/predict-from-location` wrapping `InferencePipeline` (see TASK.md v2 notes)
