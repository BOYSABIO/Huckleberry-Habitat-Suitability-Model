# Slim dependencies for the feature-based API (TASK.md Phase 2+).
# Use this for local API dev and Docker — not the full ETL / GridMET stack.
numpy>=2.0.0
pandas>=2.2.0
scikit-learn>=1.6.0
joblib>=1.5.0
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
